version: '3.9'

services:
  notification-service: 
    container_name: notification-service
    image: budgetro/notification-service:dev
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env.production.local
    networks:
      - budgetro

networks:
  budgetro:
    external: true