version: '3.9'

services:
  users-service: 
    container_name: users-service
    image: budgetro/users-service:dev
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env.production.local
    networks:
      - budgetro

networks:
  budgetro:
    external: true